json.extract! owner, :id, :header, :title, :recepient, :name, :first, :paragraph, :second, :paragraph, :first, :person, :name, :first, :position, :created_at, :updated_at
json.url owner_url(owner, format: :json)
